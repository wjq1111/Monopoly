# 开发、素材、写作与提交

## 工作范围与依据

[策划规则整理](docs/故事审阅与开工前条件.md) 记录故事及已确认的规则；[技术方案与实施计划](docs/技术方案与实施计划.md) 记录如何实现、先后顺序和当前完成范围。用户的新确认优先，应同步回对应文档。

先阅读相关规则，再完成当前阶段可验证的改动。尚未确认的规则写明“待定”，不得悄悄变成默认机制。技术选型可以依据当前目标作合理决定，但需要说明影响；技术决定与艺术表现都不应扩展故事内容。

## 各专业交付

| 职责 | 交付内容 | 检查重点 |
| --- | --- | --- |
| 策划 | 已有剧情、规则、事件条件、数值和待定问题 | 原稿依据、随机行动下的逻辑完整性、主线与个人事件顺序 |
| 程序 | Godot 场景、规则状态、交互、后续联机与存档 | 输入是否合法、状态是否正确、是否重复结算、实际运行 |
| 美术 | 三只猫、场景、棋盘、图标等当前阶段需要的素材 | 角色一致性、透明边缘、实际游戏尺寸下的清晰度 |
| 界面交互 | Godot 中可操作的界面与反馈 | 中文排版、选中、禁用、等待状态和窗口适配 |
| 动画特效 | 移动、动作、命中与事件反馈等已有玩法的表现 | 触发时机、完成与中断、遮挡及规则结算不受动画时长影响 |
| 音频 | 分开管理音效、音乐、环境声和配音 | 播放、循环、淡入淡出、音量与静音的实际试听 |
| 验收 | 当前阶段可运行的成果与检查记录 | 规则、画面、操作和声音是否共同呈现约定效果，记录尚未覆盖项 |

职责说明不代表这些内容已经制作完成。交付状态应以实际文件、引擎接入情况和检查记录为依据。

## 风格依据

美术、界面、动画和音频任务开始前，先读 [素材风格与生成规范](docs/素材风格与生成规范.md)。该文件统一维护风格版本、固定基准、生成约束、界面参数、声音参考及一致性检查；本文件维护通用交付与验收要求。后续产物沿用已确认基准，候选素材不会自动成为新基准。

当前画风仍待定，编写规范不代表已经确定艺术方向。首次风格样张及后续风格变更，以用户已确认方向或新要求为依据；需要选择新方向时，先准备少量可比较的角色、界面或声音样例，再请用户决定并记录基准，然后批量推进。常规整理、修正与接入沿用既有方向，不增加重复审批。

## 美术、界面与动画

生成或编辑位图使用当前可用的图像生成能力，并按该能力的技能要求操作。角色样张应固定花纹、体形比例、配色和辨识特征；后续姿态、表情及动画保持一致，不用外观变化引入新身份或故事。

检查透明素材在明暗底色上的边缘、裁切与对齐；以实际游戏尺寸检查棋子、表情和图标是否清楚。原始大图与用于运行的尺寸应能对应，导出后还要查看 Godot 导入与显示结果。

界面效果图只用于表达方向。最终界面须在 Godot 中实际操作，检查中文字体、换行、长文本、信息层次，以及选中、禁用、等待和必要的错误反馈；在项目支持的窗口尺寸下检查缩放、遮挡和可点击区域。

动画和特效应与实际状态变化对应。检查连续触发、被打断和场景切换后的表现；视觉完成不能成为唯一的规则结算依据。只对已经实现的行为验收，不用演示动画宣称规则已经可用。

## 音频制作与检查

音效、音乐、环境声和配音分别记录用途与进度。短音效可以通过程序合成、剪辑与参数调整制作，再接入 Godot；这不等于已有完整音乐或配音制作能力。

当前没有音乐或配音的专用生成工具，不承诺一次生成专业原声或完整配音。相关工作可以先完成声音需求、节奏与情绪说明、配音文字、占位素材及播放接入；采用现有素材时记录来源与可用范围。

音频接入后，实际试听播放触发、重复触发、循环衔接、淡入淡出、各类声音的音量关系及静音。需要停止的声音应在事件结束或场景切换后停止。未实际试听时，只能报告文件参数、资源引用或播放逻辑的结构检查，不能宣称听感、循环质量或混音已通过验收。

## 素材记录与状态

在相应交付记录中注明素材的文件位置、用途、来源、可用范围及状态。来源包括原稿、自制、生成所用工具与参考，或外部素材的原始页面；外部素材同时保留可核对的许可说明。来源或可用范围未核实的素材写明待核实，不默认可随游戏分发。

素材状态使用以下含义：

| 状态 | 含义 |
| --- | --- |
| 占位 | 用于验证流程，效果或可用范围尚不满足最终要求 |
| 候选 | 已整理、可供比较，尚未确定作为最终素材 |
| 已接入 | 已放入 Godot 对应场景或播放流程，仍可能需要表现调整 |
| 已验收 | 已完成该专业要求的实际显示、操作或试听检查，并记录结果 |

生成了一张图或一个声音文件，只能说明产生了素材。不要直接将其记为已接入或已验收；替换素材后，重新检查受影响的表现。

## 代码约定

- 使用 Godot 4.6.1 和带类型的 GDScript。文件、函数和成员使用 `snake_case`，类名使用 `PascalCase`，常量使用 `UPPER_SNAKE_CASE`；GDScript 用制表符缩进。
- 规则及状态在 `src/domain/`，启动和界面在 `src/app/`，可编辑规则资源在 `data/rules/`。后续确有模块需要时再扩充目录。
- 界面不直接修改经验、饱食度或故事进度。一次规则变化应有明确输入、合法性判断和状态结果，便于本地验证及后续由房主统一结算。
- 饱食度内部使用整数半点单位，显示时换算；不要混用显示值和内部单位。用字段名或类型明确单位。
- 注释解释规则来源、边界或必要的实现原因，不逐行复述代码。尚未实现的能力不能在界面或说明中写成可用。

## 文档写作

先写具体结果，再说明依据和验证。中文叙述使用故事原有称呼，例如“三花”“布偶”“奶牛”“饱食度”“野性”。

策划变更记录确认后的规则与仍待决定的问题；技术文档记录职责、状态流、实施顺序和验收条件。不要把设计建议写成已确认事实，也不要把计划写成完成记录。引用故事时指向现有文档或截图页码，引用引擎行为时优先使用对应版本的官方文档。

与行为或实际表现有关的变化应同步相关文档。无需为纯文档调整编写测试，也无需只为措辞修改运行完整 Godot 检查。

## 工作与决策记录

每轮执行工作开始时，在 [工作与决策记录](docs/工作与决策记录.md) 登记唯一工作 ID、用户要求和当前范围；执行中补充决定与变化，结束时填写实际交付和验证。逐轮记录不等于逐轮全测，未到需求验收时按实际写明针对性检查或未运行。本轮没有新增助手决定时写明“无”，不能只记录代码文件而漏掉技术、视觉、音频或流程选择。

每项助手决定记录决定内容、依据、影响、决定人，以及“助手临时选择”“用户已确认”等实际状态。助手在授权范围内作出的实现选择仍由助手作出，不能改写为用户确认；用户明确给出的玩法规则应记为用户决定。用户后续确认、修改或否定时追加结果并关联原决定，保留原记录。

每条工作记录包含：完成内容、未做内容及原因、阻塞原因（没有则注明无）、需求 ID 与版本快照、用例 ID、实际运行 ID 与证据位置、剩余问题和下一步。未生成的关联或证据写“待补充”“未运行”或具体缺口，不能填写虚构值。

历史工作只根据现有对话、文档和产物补记，明确标注“历史补记”及来源。没有逐轮日志时不伪造轮次和时间，没有原始运行报告时不把旧总结扩写为逐例通过，没有用户反馈时不补造认可。补记与本轮重新执行的检查分开记录。

## 从需求到验收

测试任务先读 [测试流程与用例规范](docs/testing/测试流程与用例规范.md)，在 [需求追踪矩阵](docs/testing/需求追踪矩阵.md) 查找来源、范围与覆盖状态；脚手架用例见 [M0 测试用例](docs/testing/M0测试用例.md)。详细测试格式与状态语义集中维护在测试规范，不另建重复规则。

测试以可验收需求为单位安排。需求开发中只按风险做必要的针对性检查，不在每次改动后运行完整流程；一项需求的约定行为完成后，执行对应需求用例及受影响的回归。完整自测留给阶段验收或跨模块整体验证，不因一次文档、布局或局部代码修改自动触发。

先从需求条目制定用例，再编写或选择检查实现。每例关联需求 ID、来源条目和需求版本快照，并记录前置条件、步骤、预期结果、实测结果、证据及逐例状态。需求变化后保留旧版本依据，更新受影响用例；不得为迎合现有代码而悄悄改写预期。

用例覆盖应验证可观察行为或关键约束，例如角色唯一性、行动顺序、饱食度边界和同一事件重复结算。一个汇总断言数量不能替代逐例记录。未实现、阻塞、未运行的项目保留各自状态，不算通过；失败修复后记录新一次运行与结果，不覆盖旧失败证据。

需要导入、领域或入口快速检查时使用：

```powershell
.\scripts\check.ps1
```

阶段验收或跨模块整体验证时使用完整自测入口：

```powershell
.\scripts\self_test.ps1
```

自动定位不到引擎时，在所用脚本后加上 `-GodotPath 'E:\MyGames\Godot_v4.6.1-stable_win64.exe'`。脚本是否已完成及本轮实测状态以对应工作记录为准；命令存在不代表运行通过。

快速检查包含资源导入、领域断言和入口冒烟，可按当前风险和检查目标选用；不要求每次代码修改都执行该入口。需求验收只覆盖对应需求与受影响回归，完整阶段验收不能仅凭快速检查结果替代。领域规则可无界面测试；界面验收必须实际启动 Godot 渲染场景，并通过正常输入路由完成操作，不能仅发送 `pressed` 信号冒充点击。引擎内注入输入事件与操作系统层真实鼠标操作分别说明，未覆盖后者时不得宣称已覆盖。

真实运行记录引擎版本、代码与需求版本、运行入口、输入方式和逐例结果，保留可定位的日志、截图或录像。截图和录像支持核查，不自动证明行为正确；仍须对照步骤与预期核实状态变化。声音另需实际试听，未听只能报告结构检查。

完成时报告实际运行内容、证据位置和未覆盖项。仅文档或布局调整检查来源、链接、术语与记录一致性，以及受影响的实际布局即可，不运行完整流程；如改变了交互或规则行为，在该需求完成时执行相应用例与回归。

## 提交约定

Git 提交与推送的执行入口见 [项目 Git 技能](.agents/skills/our-home-git/SKILL.md)，本节统一维护提交规则。

一个提交围绕一个可说明的目的。提交前查看 `git status --short`、`git diff`，暂存后核对 `git diff --cached`。执行 `git diff --check`；已有暂存内容还应执行 `git diff --cached --check`。按明确文件或差异暂存，避免夹带其他人的工作，不重置已有暂存内容。

按用户本次要求选择工程代码、文档、素材和仓库内技能；完整项目交付包含对应的 `.agents/skills/`。不因文件未被 Git 跟踪就漏掉它，也不把个人全局技能或项目外文件纳入上传。已有暂存内容不在本次范围时，用限定路径或独立索引组织提交，保留原有暂存内容。

不提交 `.godot/`、临时日志、导出产物、访问令牌或本地密钥。Godot 的 `.gd.uid` 属于项目资源标识，应随对应脚本保留。

标题格式采用 `类型(范围): 中文说明`，范围可省略：

| 类型 | 用途 |
| --- | --- |
| `feat` | 新增可用能力 |
| `fix` | 修复现有行为 |
| `docs` | 文档调整 |
| `refactor` | 保持行为的代码整理 |
| `test` | 测试补充或调整 |
| `chore` | 工程配置、脚本和维护 |

例如：`chore(project): 搭建 Godot 启动与验证脚手架`。

提交正文按需要说明改了什么、为什么及验证结果；存在限制时明确写出。简单改动保持简短。提交说明、合并请求描述和交付说明均只声明实际完成的内容。

当前任务未要求提交时，保留可审查的工作区改动即可；已明确授权提交时按授权完成。不要编造 Git 身份或远程地址，也不要将提交授权视为推送、发布或改写历史的授权。

用户明确要求上传或推送，且已指定或确认本次目标与范围时，完成检查和提交后直接执行，不重复索取同一授权。推送前获取远程分支状态，保留已有历史；普通上传不使用强制推送。遇到认证、权限或分支保护限制时说明具体原因，保留本地提交。推送后核对远程提交号与本地状态，报告实际上传内容和未上传部分。
